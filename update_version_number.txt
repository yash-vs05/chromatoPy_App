# Change this number each time
new_version_number="0.1.8"

source ~/uv-chromatopy/bin/activate
cd /Users/gerard/Documents/GitHub/chromatoPy_App

python3 scripts/set_version.py "$new_version_number"

git status
git add .
git commit -m "Bump version to v$new_version_number"

git push origin main

git tag "v$new_version_number"
git push origin "v$new_version_number"